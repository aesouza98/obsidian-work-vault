filters:
  and:
    - categories.contains(link("Tech"))
    - '!file.name.contains("Template")'
properties:
  file.name:
    displayName: Name
  note.type:
    displayName: Type
  note.topics:
    displayName: Topics
  note.last:
    displayName: Last
views:
  - type: table
    name: All
    order:
      - file.name
      - type
      - topics
      - last
    sort:
      - property: type
        direction: ASC
      - property: last
        direction: DESC
      - property: file.name
        direction: ASC
  - type: table
    name: Type
    filters:
      and:
        - list(type).contains(this)
    order:
      - file.name
      - topics
      - last
    sort:
      - property: last
        direction: DESC
      - property: file.name
        direction: ASC
  - type: table
    name: Topics
    filters:
      and:
        - list(topics).contains(this)
    order:
      - file.name
      - topics
      - last
    sort:
      - property: last
        direction: DESC
      - property: file.name
        direction: ASC
